let num = parseInt(prompt("Ingresa un número:"));
let divisores = [];

for (let i = 1; i <= num; i++) {
    if (num % i === 0) {
        divisores.push(i);
    }
}

document.write("Los divisores de " + num + " son: " + divisores.join(", "));
