let num = parseInt(prompt("Ingresa un número:"));
let divisible = false;

if (num % 2 === 0 || num % 3 === 0 || num % 5 === 0 || num % 7 === 0) {
    divisible = true;
}

document.write(divisible ? "El número es divisible por 2, 3, 5 o 7." : "El número no es divisible por 2, 3, 5 ni 7.");
